<% if header.present? %>
  <div class="jumbo highlight">
    <div class="row">
      <div class="small-12 medium-9 column small-centered text-center">
        <p class="lead"><%= image_tag(image_path_for("logo-lema.svg"), width: 300) %></p>
        <div class="small-12 medium-6 large-4 small-centered">
          <%= link_to header.link_text, header.link_url, class: "button expanded large" %>
        </div>
      </div>
    </div>
  </div>
<% end %>
